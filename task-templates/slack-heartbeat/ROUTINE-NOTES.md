
Monitor Slack continuously, every 10 minutes during business hours (8:00–18:00 HST, Mon–Fri).

**Workflow:** see `agents/slack-liaison/AGENTS.md`.

**Output:** Slack Report to shared state; CLG flags escalated immediately to Chief of Staff.

**Runtime trigger:** see `.paperclip.yaml` → `routines.slack-heartbeat`.
